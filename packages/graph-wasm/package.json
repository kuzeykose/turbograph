{
  "name": "@workspace/graph-wasm",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "cargo build --release --target wasm32-unknown-unknown && cp target/wasm32-unknown-unknown/release/graph_wasm.wasm ../../apps/dashboard/public/graph.wasm",
    "check-types": "cargo check --target wasm32-unknown-unknown"
  }
}
